{
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "13.35.1"
  },
  "name": "pokerten-security-checks",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.security.json --project demo-pokerten-security --only firestore \"node --test rules.test.cjs\""
  }
}
